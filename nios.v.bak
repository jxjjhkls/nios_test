   
module mynios(clk,rst);
input clk;
input rst;
  niso u

	niso u0 (
        .clk_clk       (clk),       //   clk.clk
        .reset_reset_n (<connected-to-reset_reset_n>)  // reset.reset_n
    );

endmodule